def common(a):
    prefix = a[0]
    for i in a[1:]:
        while not i.startswith(prefix):
            prefix = prefix[:-1]
            if not prefix:
                return""
    return(prefix) 

a = ["flower","flow","flight"] 
output = common(a)
print(output)


time complexity is O(n * m) 
space complexity is :O(1) 
